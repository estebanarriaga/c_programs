// Programa que concatene dos cadenas de caracteres:
// Utilizando un array y punteros


// Esto es sin punteros
// Utilizando dos cadenas:
// while(count != '\0'){ cadena1[x]=cadena2[x]; x++}
// Se puede crear una tercera cadena en la que concatenar las otras dos
// while(cad2[i] != '\0'){i++}
// (Mas o menos)


#include <stdio.h>
#define TAM 100

int main(){
	char array[TAM];
	char *parray = array;
	int count = 0,  x = 0;
	char c;

	printf("Introduce una cadena: \n");
	gets(text);	
		
	printf("Introduce una segunda cadena: \n");
	gets(text);	
	
	while((c=*(str++)) != '\0'){
               	count++;
        }

	for(int i; i<=(count+1); i++){
		
	}
	
	

	


}
