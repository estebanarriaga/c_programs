// 2 Cadenas en C. Lectura de cadenas.

#include <stdio.h>
#define TAM 20

void main(){
	char nombre[TAM];
	char apellido[TAM];
	char newChar;
	int strSize
	int i;

	while((newChar=getchar()) != '\n'){
		nombre[strSize] = 
	
	printf("EScribe tu nombre: \n");
	scanf("%20[^\n]s", &array1);

	printf("%s", array1);
}
